apiVersion: 1

datasources:
  - name: Prometheus
    uid: af17ki1wwudj4c   # ← este UID debe coincidir con el del dashboard blastnodes.json
    type: prometheus
    url: http://prometheus:9090
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 5s